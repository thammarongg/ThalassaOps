{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local ThalassaOps shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
